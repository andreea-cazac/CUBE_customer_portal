const { defineConfig } = require("cypress");

module.exports = defineConfig({
  projectId: '7accug',
  component: {
    devServer: {
      framework: "vue-cli",
      bundler: "webpack",
    },
  },

  component: {
    devServer: {
      framework: "vue-cli",
      bundler: "webpack",
    },
  },
});
